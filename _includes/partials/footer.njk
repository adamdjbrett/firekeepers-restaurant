<footer class="container-fluid grape">
<div class="row mt-5 ">
<div class="col-md-12 p-3 p-md-5 text-center"><a href="{{metadata.url}}" title="{{metadata.title}}">
<img class="img-fluid" width="100" height="60" src="{{metadata.footer.icon}}" alt="{{metadata.footer.title}}"/></a>
<h2 class="f-text text-uppercase text-center"><strong><a href="{{metadata.url}}" class="none">
{{metadata.footer.title}}
</a></strong></h2>
<nav class="nav justify-content-center">
{% for n in metadata.footer.nav %}<a class="nav-link" aria-current="page" href="{{n.url}}">{{n.nav}}</a>{% endfor %}
</nav>
</div>
<p class="col-md-12 p-1 text-center"><small>
{{metadata.copyrights}} | {{metadata.developer.text}} <a href=""{{metadata.developer.url}}>{{metadata.developer.name}}</a>
</small></p>
</div>
</footer>